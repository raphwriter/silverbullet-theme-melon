```space-style

html[data-theme="light"] body div#root.sb-preview {
  font-family: var(--editor-font);
}

html[data-theme="light"] body #root.sb-preview :is(h1, h2, h3, h4, h5, h6) {
  font-family: var(--ui-font);
}

html[data-theme="light"] body #root.sb-preview :is(h1, h2, h3, h4, h5, h6, span.p, li) {
  color: var(--root-color);
}

html[data-theme="light"] body #root.sb-preview :is(span.p, li) {
  
}

html[data-theme="light"] body #root.sb-preview :is(pre) {
  font-family: var(--code-font);
  color: var(--root-color);
}

```
